"""experiments/specs — backward-compat shim after Tier 0c.2 spec promotion.

The canonical specs moved to `app/specs/`. This module re-exports them from
their new home so the canvas probe and canvas_eval scripts under
`experiments/` continue to work without each having to update its imports.

Also exports the prompt templates and renderers that still live here:
those will relocate in Tier 6 (judges) to `app/prompts/` and `app/tools/render/`
per ADR-0007.
"""
from __future__ import annotations

from app.specs import (
    # Enums
    Area,
    CheckOutcome,
    ClassificationCheck,
    FieldConfidence,
    FieldScope,
    JudgeType,
    LabelMatchMode,
    PhaseAction,
    PliAxis,
    PliMode,
    ReadDirection,
    ValueDtype,
    ValueDtypeMode,
    ValuePattern,
    Verdict,
    # Spec dataclasses
    FieldSpec,
    StageSpec,
    SubfieldSpec,
    ValueConstraints,
    # Identifier specs
    COLOR_CODE_SPEC,
    COLOR_NAME_SPEC,
    DATE_IDENTIFIERS_AT_LEAST_ONE,
    DELIVERY_DATE_SPEC,
    EX_FTY_DATE_SPEC,
    FABRIC_CODE_SPEC,
    FABRIC_NAME_SPEC,
    IDENTIFIER_CANONICALS,
    IDENTIFIER_SPECS,
    IO_NUMBER_SPEC,
    MANDATORY_IDENTIFIERS,
    QUANTITY_SPEC,
    SHIPMENT_DATE_SPEC,
    STYLE_CODE_SPEC,
    STYLE_NAME_SPEC,
    get_identifier_spec,
    # Stage specs
    CUTTING_SPEC,
    EX_FACTORY_SHIPMENT_SPEC,
    FABRIC_SPEC,
    FEEDING_SPEC,
    FINAL_INSPECTION_SPEC,
    FIT_APPROVAL_SPEC,
    FIT_SEND_SPEC,
    IN_HOUSE_FABRIC_SEND_SPEC,
    LINE_PLAN_SPEC,
    PRE_PRODUCTION_SEND_SPEC,
    SEWING_END_SPEC,
    SEWING_SPEC,
    SEWING_START_SPEC,
    SIZESET_SUBMISSION_SPEC,
    STAGE_CANONICALS,
    STAGE_SPECS,
    VAP_SPEC,
    get_stage_spec,
    # Sub-field specs
    ACTUAL_DATE_SPEC,
    APPROVAL_DATE_SPEC,
    APPROVED_QTY_SPEC,
    END_DATE_SPEC,
    PLANNED_DATE_SPEC,
    QUANTITY_SUBFIELD_SPEC,
    RECEIVED_DATE_SPEC,
    REMARKS_SPEC,
    START_DATE_SPEC,
    STATUS_SPEC,
    SUBFIELD_CANONICALS,
    SUBFIELD_SPECS,
    get_subfield_spec,
    # Metadata specs
    BUYER_SPEC,
    FACTORY_SPEC,
    METADATA_CANONICALS,
    METADATA_SPECS,
    ORDER_RECEIPT_DATE_SPEC,
    SEASON_SPEC,
    get_metadata_spec,
    # Schemas
    ClassificationJudgeInput,
    ClassificationJudgeOutput,
    DetectedBand,
    FieldFindingJudgeInput,
    FieldFindingJudgeOutput,
    FieldLocator,
    FinalFinding,
    FinalStage,
    FindingForJudge,
    FindingWithJudgment,
    JudgmentRec,
    MetadataEntry,
    MetadataKVJudgeInput,
    MetadataKVJudgeOutput,
    PhaseIdentifierJudgeInput,
    PhaseIdentifierJudgeOutput,
    PhaseMetadataJudgeInput,
    PhaseMetadataJudgeOutput,
    PhaseStageJudgeInput,
    PhaseStageJudgeOutput,
    PliEnumerationPlan,
    PliGroup,
    ReadPattern,
    SampleCell,
    SampleRow,
    SheetLevelPlan,
    SheetSample,
    SpecSummary,
    StageBandJudgeInput,
    StageBandJudgeOutput,
    StageLocator,
    StageStrip,
    SubColumnRef,
)
from experiments.specs.prompts import (
    CLASSIFICATION_JUDGE_PROMPT,
    FIELD_FINDING_JUDGE_PROMPT,
    METADATA_KV_JUDGE_PROMPT,
    PHASE_IDENTIFIER_JUDGE_PROMPT,
    PHASE_METADATA_JUDGE_PROMPT,
    PHASE_STAGE_JUDGE_PROMPT,
    STAGE_BAND_JUDGE_PROMPT,
)
from experiments.specs.render import (
    render_classification_judge_prompt,
    render_field_finding_judge_prompt,
    render_metadata_kv_judge_prompt,
    render_phase_identifier_judge_prompt,
    render_phase_metadata_judge_prompt,
    render_phase_stage_judge_prompt,
    render_stage_band_judge_prompt,
    spec_summary_for_field,
    spec_summary_for_stage,
    spec_summary_for_subfield,
)
