"""Structure-layer policies — score candidates for tier 1 structural decisions.

Each policy returns a `PolicyVerdict` for one candidate (typically a row,
column, or rectangle). Pickers in `app/components/pickers/` compose them.
"""
